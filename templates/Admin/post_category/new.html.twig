{% extends 'base.html.twig' %}
{% block title %}
    New PostCategory
{% endblock %}
{% block body %}
    <div class="max-w-4xl mx-auto mt-24 p-6 bg-primary-light_white rounded-lg shadow-lg text-primary-dark">
        <h1 class="text-3xl font-bold mb-6 text-center">
            Create new Post Category
        </h1>
        <!-- Form include with TailwindCSS styles -->
        <div class="mb-6">
            {{ include('Admin/post_category/_form.html.twig') }}
        </div>
        <!-- Back to list button -->
        <div>
            <a href="{{ path('app_post_category_index') }}" class="text-primary-blue">
                Back to list
            </a>
        </div>
    </div>
{% endblock %}
